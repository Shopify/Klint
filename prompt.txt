My project repository is growing and this is my first open-source project, so i'm still learning. 
I would like to divide this repo so user can get various parts from npm.

so the main package is @shopify/klint
the klint package should include klint.tsx, klint functions.tsx and the Elements

but now that we have more collaterals, i would like to be able to use 
npm i @shopify/klint/mcp
npm i @shopify/klint/editor
npm i @shopify/klint/web-component
npm i @shopify/klint/docs
npm i @shopify/klint/plugins
etc ... like i would like to make a plain TS version of this library at some point, so this pattern should be able to scale.

I don't want users to have to download the whole bundle and keep the main Klint library very lightweight.
What do you recomend ? 
